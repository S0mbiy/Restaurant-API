In order to run this code a venv with the libraries under requirements.txt are needed, once the virtual environment is
created and activated in a terminal run:
pip install -r requirements.txt
After this execute the RestaurantAPI with:
python RestaurantAPI.py
And lastly run the Client in another terminal with:
python Client.py
